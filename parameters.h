#ifndef DEMO2_PARAMETERS
#define DEMO2_PARAMETERS

#include "types.h"

//Scene parameters
int polygonCount;
polygon * scene;
int verticesCount;

//Program parameters
enum {
    SCREEN_WIDTH = 800,
    SCREEN_HEIGHT = 600,
};

#endif
